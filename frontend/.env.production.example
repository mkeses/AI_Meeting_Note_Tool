# The PWA build uses its own HTTPS origin for /api and /ws by default.
# Leave this unset for the supplied Nginx deployment. Set it only when the
# frontend is intentionally hosted on a different origin.
# VITE_BACKEND_URL=https://api.example.com
